## Design delta — ask

**Question:** Does the variant selection for `mode` vary per request (input data), per deployment (fixed at startup), or via a runtime toggle? Code trace: `mode` does not resolve to a request parameter or a config property in the provided files (not wired, or named as a concept rather than a code token).
